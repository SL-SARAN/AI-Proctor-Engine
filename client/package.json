{
  "name": "proctoring-engine-client",
  "version": "0.1.0",
  "private": true,
  "description": "Browser capture client for the AI Proctoring Engine. Vanilla TypeScript + Vite. Sends sparse telemetry, audio chunks, and browser events over an authenticated WebSocket; renders the kill-switch UI on flag confirmation.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.18"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "jsdom": "^25.0.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
